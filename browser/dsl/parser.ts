import type { BrowserWorkflow } from "./types.js";

export function parseWorkflow(raw: unknown): BrowserWorkflow {
  // TODO: validate and parse raw DSL input into a BrowserWorkflow
  throw new Error("Not implemented");
}
